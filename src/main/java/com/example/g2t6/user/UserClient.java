package com.example.g2t6.user;
/*
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.web.client.RestTemplateBuilder;
import org.springframework.context.annotation.Bean;
import org.springframework.web.client.RestTemplate;
import com.example.g2t6.user.*;

public class UserClient {

    private RestTemplate template;
    
    @Autowired
    void setTemplate(final RestTemplate template) {
        this.template = template;
    }


    @Bean
    RestTemplate restTemplate(RestTemplateBuilder builder) {
        // Other configurations here if needed
        return builder.build();
    }

    public User getUser(final String URI) {
        final User user = template.getForObject(URI + "/", User.class);
        return user;
    }

}
*/

